# Vera — magicpin AI Challenge
# Production dependencies

# Claude API (direct) + OpenRouter backend
anthropic>=0.40.0
openai>=1.50.0

# FastAPI + server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Async HTTP (for future outbound webhook calls)
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Benchmarking / dev
python-dotenv>=1.0.0
